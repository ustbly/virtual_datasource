syntax = "proto3";

// 选项配置
option java_package = "proto_compile.cetc41.nodecontrol";

enum CmdType
{
    RESERVED = 0;
    PAUSE = 1;
    RESUME = 2;
    RESTART_AT = 3;
    RESET_LENGTH = 4;
    STARTUP = 5;
    SHUTDOWN = 6;
}

message Result
{
    repeated int32 num_seq = 1;
}